#include "Subject.h"
#include "Observer.h"

//void Subject::attach(Observer *obs) {
//  views.push_back(obs);
//}
//
//void Subject::notify() {
//  // 5. Publisher broadcasts
//  for (auto it = views.begin(); it != views.end(); it++)
//      (*it)->update(this);
//}

